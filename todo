Error handling:
[ ] wrong ticker name, or 404
[ ] we have one ticker on one stock-exchange, but don't have it on another

Architecture
[+] implement price adapter (move this logic from kucoin to 'adapter')

Figure it out:
[ ] Kucoin's api doesn't provide full depth of dom, instead,
    i'm pulling up response with 1 depth, api could request 
	an arbitrary depth